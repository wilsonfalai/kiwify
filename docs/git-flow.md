# Git Flow

The repository uses Git Flow with:

- `main` for release-ready code.
- `develop` for integration work.
- feature branches for implementation tasks.
- pull requests with CI passing before merge.

Implementation work should branch from `develop` and merge back through review.
